# === Database (Vercel Postgres) ===
# These will be auto-populated by Vercel after adding Postgres database
POSTGRES_URL=postgresql://user:password@localhost:5432/dbname
POSTGRES_PRISMA_URL=postgresql://user:password@localhost:5432/dbname?pgbouncer=true
POSTGRES_URL_NON_POOLING=postgresql://user:password@localhost:5432/dbname

# === Payload CMS ===
# Generate with: openssl rand -hex 32
PAYLOAD_SECRET=your-secret-here
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# === Cloudflare R2 (S3 API) ===
# Get these from Cloudflare R2 Dashboard > Manage R2 API Tokens
R2_BUCKET_NAME=your-bucket-name
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com

# === Optional: R2 Public URL ===
# If using custom domain or R2 dev subdomain for public access
# R2_PUBLIC_URL=https://cdn.yourdomain.com
# OR
# R2_PUBLIC_URL=https://pub-<hash>.r2.dev
